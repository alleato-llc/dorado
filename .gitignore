# Build output (the main workspace and the separate fuzz workspace).
/target
/fuzz/target

# Fuzzing corpora and crash artifacts.
/fuzz/corpus
/fuzz/artifacts

# Cargo.lock is committed: this workspace ships binaries (the CLI and GUI), so a
# pinned, reproducible dependency set is wanted.
