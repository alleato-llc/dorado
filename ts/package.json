{
  "name": "@dorado/ts",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "TypeScript port of dorado, matching the Rust/Go implementations.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.8"
  }
}
