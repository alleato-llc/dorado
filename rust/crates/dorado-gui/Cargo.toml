[package]
name = "dorado-gui"
description = "Graphical frontend for dorado (educational, unaudited)."
version = "0.1.0"
edition = "2021"
license = "MIT"

[[bin]]
name = "dorado-gui"
path = "src/main.rs"

[dependencies]
dorado-engine = { path = "../dorado-engine" }
dorado-gui-kit = { path = "../dorado-gui-kit" }
# The house component kit + theming. A sibling repo; path dep for local dev.
# iced is re-exported as rime::iced, but we also depend on it directly for the
# application entry point; cargo unifies to one 0.14.x across the graph. The
# `tokio` feature (on top of iced's own defaults, which already include
# `thread-pool` and so give `Task::perform` a working executor) is needed
# specifically for `iced::time::every`, which drives the busy-progress tick;
# the default (featureless) executor's timer module is empty.
rime = { path = "../../../../rime/rime" }
iced = { version = "0.14", features = ["canvas", "advanced", "tokio"] }
# Native Open/Save file dialogs; iced has none.
rfd = "0.15"
# Wipes the worker's copy of the password bytes (the live password is held by
# rime's locked SecretHandle). Already in the tree via dorado-engine.
zeroize = "1"
# PNG encoder for the env-gated review-screenshot harness (src/shot.rs).
png = "0.17"
# Best-effort process hardening (src/harden.rs): suppress core dumps and, on
# Linux, refuse same-user ptrace. A safe wrapper over the raw syscalls (its own
# unsafe, not ours, so the crate keeps `#![forbid(unsafe_code)]`, exactly as
# `region` does for the CLI's mlock). Already in the tree via iced/winit/wgpu;
# this only turns on its `process` feature.
rustix = { version = "1", features = ["process"] }
